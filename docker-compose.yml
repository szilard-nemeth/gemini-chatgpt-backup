version: "3.9"

services:
  gemini-export:
    build: .
    container_name: gemini-export
    env_file:
      - .env
    volumes:
      - ./publix_export:/app/publix_export
    command: ["poetry", "run", "python", "publix_export_playwright_structured.py"]
